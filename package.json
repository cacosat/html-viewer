{
  "name": "html-viewer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply html-viewer-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply html-viewer-db --remote",
    "tail": "wrangler tail"
  },
  "devDependencies": {
    "wrangler": "^4.40.0"
  }
}
